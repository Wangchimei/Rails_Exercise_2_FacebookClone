<div>
  <%= avatar(comment.user, "show-profile") %>
  <%= distance_of_time_in_words(comment.created_at, Time.now)%>前
  <%= comment.user.name%>
  <%= comment.content %>
  <% if current_user.id == comment.user_id %>
    <%= link_to 'Delete', [comment.feed, comment],
                method: :delete, data: { confirm: 'コメントを削除しますか？' } %>
  <%end%>
</div>
